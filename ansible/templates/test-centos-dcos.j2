#!/bin/bash

PLATFORM=`uname -ra`
if [[ $PLATFORM == *"Darwin"* ]]; then 
  open https://{{ inventory_hostname }}
elif [[ $PLATFORM == *"Microsoft"* ]]; then 
  cmd.exe /c 'start chrome https://{{ inventory_hostname }}'
else
  nohup google-chrome https://{{ inventory_hostname }} &
fi 
