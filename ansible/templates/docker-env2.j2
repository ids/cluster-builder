#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

export DOCKER_CERT_PATH=$DIR/api-certs
export DOCKER_TLS_VERIFY=1
export DOCKER_HOST={{ inventory_hostname }}:{{ docker_tls_port }}

#
# Bundle for remote API access
# Generated by Cluster Builder for Docker CE
#
# After sourcing this env file, use "docker info" to discover the location 
# of Swarm managers and engines. and use the --host option to override $DOCKER_HOST
#
# Run this command from within this directory to configure your shell:
# eval $(<docker-env.sh)