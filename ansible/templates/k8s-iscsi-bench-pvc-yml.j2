kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: iscsi-benchmark-target-volume-claim
  annotations:
    volume.beta.kubernetes.io/storage-class: "{{ targetd_server_storage_class }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
