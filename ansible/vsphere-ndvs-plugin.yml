---
- hosts: all

  gather_facts: false
  become: true
  tasks:  
  - name: Install VMware Volume Driver Plugin on Swarm Nodes
    shell: docker plugin install --grant-all-permissions --alias vsphere store/vmware/docker-volume-vsphere:latest
